<snippet>
	<content><![CDATA[
vll grph[maxN];
ll n;
void iptree()
{
    cin>>n;
    fo(i,1,n)
    {
        ll u,v;
        cin>>u>>v;
        grph[u].pb(v);
        grph[v].pb(u);
    }
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>iptree</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
